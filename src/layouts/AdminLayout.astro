---
import '../styles/global.css';
import { getSession } from 'auth-astro/server';

interface Props {
  title?: string;
}

const { title = 'Admin Panel | Mister LyA' } = Astro.props;

const session = await getSession(Astro.request);

// Redirect if not authenticated
if (!session?.user) {
  return Astro.redirect('/login');
}

const user = session.user;

// Get current path for active nav highlighting
const currentPath = Astro.url.pathname;
---

<!DOCTYPE html>
<html class="light" lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    
    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;700;800&family=Manrope:wght@400;500;700&display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet" />
    
    <title>{title}</title>
  </head>
  <body class="bg-surface-container-low text-on-surface text-sm">
    <div class="flex min-h-screen">
      <!-- Sidebar -->
      <aside class="w-48 bg-surface border-r border-outline-variant hidden lg:flex lg:flex-col">
        <!-- Logo -->
        <a href="/" class="p-3 border-b border-outline-variant flex justify-center items-center hover:bg-surface-container-highest transition-colors">
          <img src="/logo-h.png" alt="Mister LyA" class="h-8 w-auto drop-shadow-sm" />
        </a>

        <!-- Navigation -->
        <nav class="flex-1 p-2 space-y-0.5">
          <a
            href="/admin"
            class={`flex items-center gap-2 px-2.5 py-1.5 rounded transition-all text-xs ${
              currentPath === '/admin' || currentPath === '/admin/'
                ? 'bg-secondary-container text-on-secondary-container font-semibold'
                : 'text-on-surface-variant hover:bg-surface-container-highest hover:text-on-surface'
            }`}
          >
            <span class="material-symbols-outlined text-base">
              dashboard
            </span>
            <span>Dashboard</span>
          </a>
          
          <a
            href="/admin/products"
            class={`flex items-center gap-2 px-2.5 py-1.5 rounded transition-all text-xs ${
              currentPath.startsWith('/admin/products')
                ? 'bg-secondary-container text-on-secondary-container font-semibold'
                : 'text-on-surface-variant hover:bg-surface-container-highest hover:text-on-surface'
            }`}
          >
            <span class="material-symbols-outlined text-base">
              inventory_2
            </span>
            <span>Productos</span>
          </a>
          
          <a
            href="/admin/categories"
            class={`flex items-center gap-2 px-2.5 py-1.5 rounded transition-all text-xs ${
              currentPath.startsWith('/admin/categories')
                ? 'bg-secondary-container text-on-secondary-container font-semibold'
                : 'text-on-surface-variant hover:bg-surface-container-highest hover:text-on-surface'
            }`}
          >
            <span class="material-symbols-outlined text-base">
              category
            </span>
            <span>Categorías</span>
          </a>
        </nav>

        <!-- User Info & Logout -->
        <div class="p-2 border-t border-outline-variant">
          <div class="mb-1.5 px-2">
            <p class="text-[10px] font-semibold text-on-surface truncate">
              {user.name}
            </p>
            <p class="text-[9px] text-on-surface-variant truncate">
              {user.email}
            </p>
          </div>
          <form method="post" action="/api/auth/signout">
            <button
              type="submit"
              class="w-full flex items-center gap-1 px-2 py-1 rounded bg-error-container text-on-error-container hover:brightness-95 transition-all"
            >
              <span class="material-symbols-outlined text-xs">
                logout
              </span>
              <span class="text-[10px] font-semibold">Cerrar Sesión</span>
            </button>
          </form>
        </div>
      </aside>

      <!-- Mobile Header -->
      <div class="lg:hidden fixed top-0 left-0 right-0 z-50 bg-surface border-b border-outline-variant">
        <div class="flex items-center justify-between p-3">
          <a href="/" class="flex items-center gap-2">
            <img src="/logo-h.png" alt="Mister LyA" class="h-8 w-auto" />
          </a>
          <button
            id="mobile-menu-btn"
            class="menu-btn p-2 rounded bg-primary-container text-on-primary-container transition-all"
            aria-label="Abrir menú"
          >
            <span class="material-symbols-outlined text-xl">
              menu
            </span>
          </button>
        </div>
      </div>

      <!-- Mobile Menu -->
      <div
        id="mobile-menu"
        class="lg:hidden fixed inset-0 z-40 bg-surface transform -translate-x-full transition-transform duration-300"
      >
        <div class="flex flex-col h-full">
          <div class="p-3 border-b border-outline-variant flex items-center justify-between">
            <a href="/" class="flex items-center gap-2">
              <img src="/logo-h.png" alt="Mister LyA" class="h-8 w-auto" />
            </a>
            <button
              id="mobile-menu-close"
              class="p-1.5 rounded hover:bg-surface-container-highest"
            >
              <span class="material-symbols-outlined text-on-surface text-xl">
                close
              </span>
            </button>
          </div>

          <nav class="flex-1 p-3 space-y-0.5">
            <a
              href="/admin"
              class={`flex items-center gap-2 px-3 py-2 rounded text-sm ${
                currentPath === '/admin' || currentPath === '/admin/'
                  ? 'bg-secondary-container text-on-secondary-container font-semibold'
                  : 'text-on-surface-variant'
              }`}
            >
              <span class="material-symbols-outlined text-lg">
                dashboard
              </span>
              <span>Dashboard</span>
            </a>
            
            <a
              href="/admin/products"
              class={`flex items-center gap-2 px-3 py-2 rounded text-sm ${
                currentPath.startsWith('/admin/products')
                  ? 'bg-secondary-container text-on-secondary-container font-semibold'
                  : 'text-on-surface-variant'
              }`}
            >
              <span class="material-symbols-outlined text-lg">
                inventory_2
              </span>
              <span>Productos</span>
            </a>
            
            <a
              href="/admin/categories"
              class={`flex items-center gap-2 px-3 py-2 rounded text-sm ${
                currentPath.startsWith('/admin/categories')
                  ? 'bg-secondary-container text-on-secondary-container font-semibold'
                  : 'text-on-surface-variant'
              }`}
            >
              <span class="material-symbols-outlined text-lg">
                category
              </span>
              <span>Categorías</span>
            </a>
          </nav>

          <div class="p-3 border-t border-outline-variant">
            <div class="mb-2 px-3">
              <p class="text-xs font-semibold text-on-surface">
                {user.name}
              </p>
              <p class="text-[10px] text-on-surface-variant">
                {user.email}
              </p>
            </div>
            <form method="post" action="/api/auth/signout">
              <button
                type="submit"
                class="w-full flex items-center gap-1.5 px-3 py-1.5 rounded bg-error-container text-on-error-container"
              >
                <span class="material-symbols-outlined text-sm">
                  logout
                </span>
                <span class="text-xs font-semibold">Cerrar Sesión</span>
              </button>
            </form>
          </div>
        </div>
      </div>

      <!-- Main Content -->
      <main class="flex-1 lg:pt-0 pt-14">
        <slot />
      </main>
    </div>

    <script>
      // Mobile menu toggle
      const menuBtn = document.getElementById('mobile-menu-btn');
      const menuClose = document.getElementById('mobile-menu-close');
      const mobileMenu = document.getElementById('mobile-menu');

      menuBtn?.addEventListener('click', () => {
        // Toggle menu open/close
        if (mobileMenu?.classList.contains('-translate-x-full')) {
          mobileMenu.classList.remove('-translate-x-full');
        } else {
          mobileMenu?.classList.add('-translate-x-full');
        }
      });

      menuClose?.addEventListener('click', () => {
        mobileMenu?.classList.add('-translate-x-full');
      });

      // Close menu when clicking outside or on a link
      mobileMenu?.addEventListener('click', (e) => {
        const target = e.target as HTMLElement;
        if (target.tagName === 'A') {
          mobileMenu.classList.add('-translate-x-full');
        }
      });
    </script>

    <style>
      .material-symbols-outlined {
        font-variation-settings:
          'FILL' 0,
          'wght' 400,
          'GRAD' 0,
          'opsz' 24;
      }
      
      .menu-btn .material-symbols-outlined {
        font-variation-settings:
          'FILL' 0,
          'wght' 600,
          'GRAD' 0,
          'opsz' 24;
        transition: color 0.2s ease, font-variation-settings 0.2s ease;
      }
      
      .menu-btn:hover .material-symbols-outlined {
        color: #4ade80;
        font-variation-settings:
          'FILL' 0,
          'wght' 700,
          'GRAD' 0,
          'opsz' 24;
      }
    </style>
  </body>
</html>
